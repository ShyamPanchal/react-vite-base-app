import { Translation } from "../../../utils/languages/keys";
import { OPS_KEYS } from "./keys";

export const translations: Translation = {
  [OPS_KEYS.HELLO]: "Hallo",
};

export default translations;
